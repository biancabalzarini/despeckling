\section{Materials and Methods}

\subsection{SAR Images}

For this study, two types of datasets were used: synthetically generated images and real SAR images. The synthetic dataset was created based on the $\mathcal{G}_I^0$ distribution, which models the statistical properties of speckle noise. 
A random variable $z$ following the $\mathcal{G}_I^0$ distribution can be expressed as the product of two independent random variables:
\begin{equation} \label{eq:zxy}
z = x \cdot y
\end{equation}
where $x$ represents the noise-free backscatter, modeled by an inverse Gamma distribution ($x \sim \Gamma^{-1}(-\alpha, 1/\gamma)$), and $y$ represents the speckle noise, modeled by a Gamma distribution ($y \sim \Gamma(L, 1/L)$). The parameter $\alpha$ is related to the texture or roughness of the surface, while $L$ is the number of looks of the SAR system. This model allowed us to generate pairs of clean ($x$) and noisy ($z$) images for training the proposed models. To create a robust training dataset, we generated synthetic images structured in quadrants, where each quadrant simulated a different surface texture by assigning a randomly chosen value for the $\alpha$ parameter. This approach forces the model to learn to filter noise in the presence of heterogeneous textures.

A key concept in SAR image analysis is the 'number of looks' (L), which refers to the number of independent images of the same scene that are averaged to reduce speckle noise. A higher number of looks results in a smoother image with less noise. The 'Equivalent Number of Looks' (ENL) is a widely used metric to estimate the effectiveness of a speckle filter. It quantifies the smoothness of a homogeneous area in the filtered image. A higher ENL value indicates a greater reduction in noise and, therefore, a better filter performance.

The real SAR images used in this work are from the dataset provided by \cite{vasquez2024labeled}, which contains pairs of noisy and clean images obtained through temporal averaging. An example of such a pair is shown in Figure \ref{fig:sar_images}. These images present varied textures, including urban areas, vegetation, and water bodies, which are suitable for training and testing the generalization capability of the despeckling filters.

\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{Images/noisy_ex.jpg}
        \caption{Noisy SAR image.}
        \label{fig:sar_noisy}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{Images/clean_ex.jpg}
        \caption{Clean SAR image.}
        \label{fig:sar_clean}
    \end{minipage}
    \caption{Example of a real SAR image pair (noisy and clean) from the dataset.}
    \label{fig:sar_images}
\end{figure}

\subsection{Methods}

\subsubsection{Autoencoder Architecture}
The core of our despeckling method is a convolutional autoencoder. An autoencoder is a neural network designed to learn a compressed representation (encoding) of its input and then reconstruct it (decoding). The network is composed of an encoder, which maps the input image to a lower-dimensional latent space, and a decoder, which reconstructs the image from the latent representation.

Our implementation uses a convolutional architecture. The encoder consists of a series of convolutional layers that apply filters to the input image, extracting hierarchical features and reducing spatial dimensions. The decoder uses transposed convolutional layers (also known as deconvolutions) to upsample the feature maps and reconstruct the image. The general structure of the autoencoder is symmetrical. Figure \ref{fig:esquema_red} shows a schematic of the architecture. A key aspect of our training process is that the network takes a noisy image as input and is trained to reconstruct the corresponding clean (noise-free) version, which is provided as the target output. The model is trained by minimizing the Mean Squared Error (MSE) loss between the reconstructed image and the clean target image.

\begin{figure}[htbp]
    \centering
    \includegraphics[height=3.5cm]{Images/red.png}
    \caption{Scheme of a convolutional autoencoder. The input image is progressively downsampled by the encoder to a latent representation and then upsampled by the decoder to the original dimensions.}
    	\label{fig:esquema_red}
    \end{figure}
    
    \subsubsection{Tested Architectures}
    To facilitate rapid experimentation, we developed a training framework driven by configuration files, which allowed for testing numerous architectures without modifying the source code. A key feature of this framework was its ability to automatically validate the dimensional consistency of the network layers, ensuring that all configured architectures were viable. Using this system, we designed and tested 25 distinct autoencoder configurations to identify an optimal model. The key hyperparameters for each configuration are summarized in Table \ref{tab:autoencoder_configs}. This systematic approach allowed us to explore the impact of network depth, latent space dimensionality, and training duration on despeckling performance.    
    \begin{table}[htbp]
    \centering
    \caption{Summary of all tested convolutional autoencoder configurations.}
    \label{tab:autoencoder_configs}
    \scriptsize
    \begin{tabular}{|c|c|c|c|c|c|c|}
    \hline
    \textbf{Config.} & \textbf{Conv Layers} & \textbf{Encoding Dim} & \textbf{Epochs} & \textbf{Samples} & \textbf{LR} & \textbf{Scheduler} \\
    \hline
    \hline
    \multicolumn{7}{|c|}{\textbf{Base and Reference Architectures}} \\
    \hline
    A1 & 1+MaxPool & 32 & 5 & 50K & 0.001 & None \\
    A2 & 1 & 32 & 30 & 50K & 0.001 & ELR \\
    A3 & 2+MaxPool & 512 & 500 & 50K & 0.001 & ELR \\
    \hline
    \hline
    \multicolumn{7}{|c|}{\textbf{Depth Variations}} \\
    \hline
    \multicolumn{7}{|c|}{\textit{1 Convolutional Layer}} \\
    \hline
    A4 & 1 & 32 & 500 & 50K & 0.001 & None \\
    A5 & 1 & 32 & 70 & 50K & 0.001 & None \\
    A6 & 1 & 32 & 500 & 50K & 0.0005 & None \\
    \hline
    \multicolumn{7}{|c|}{\textit{2 Convolutional Layers}} \\
    \hline
    A7 & 2 & 128 & 500 & 50K & 0.001 & ELR \\
    A8 & 2 & 128 & 70 & 50K & 0.001 & ELR \\
    A9 & 2 & 256 & 500 & 50K & 0.001 & ELR \\
    A10 & 2 & 256 & 70 & 50K & 0.001 & ELR \\
    A11 & 2 & 64 & 100 & 100K & 0.001 & RLROP \\
    A12 & 2 & 64 & 40 & 100K & 0.001 & RLROP \\
    \hline
    \multicolumn{7}{|c|}{\textit{3+ Convolutional Layers}} \\
    \hline
    A13 & 3 & 128 & 500 & 50K & 0.001 & ELR \\
    A14 & 3 & 128 & 70 & 50K & 0.001 & ELR \\
    A15 & 3 & 256 & 500 & 50K & 0.001 & ELR \\
    A16 & 3 & 256 & 70 & 50K & 0.001 & ELR \\
    A17 & 3+Dense & 128 & 500 & 50K & 0.001 & None \\
    A18 & 3+Dense & 128 & 70 & 50K & 0.001 & None \\
    A19 & 3 & 64 & 100 & 100K & 0.001 & RLROP \\
    A20 & 4+Dense & 64 & 100 & 100K & 0.001 & RLROP \\
    \hline
    \hline
    \multicolumn{7}{|c|}{\textbf{Special Training Runs}} \\
    \hline
    A21 & 1 & 32 & 15 & 50K & 0.001 & ELR \\
    A22 & 1 & 32 & 500 & 50K & 0.001 & ELR \\
    A23 & 1 & 32 & 500 & 100K & 0.001 & ELR \\
    \hline
    \hline
    \multicolumn{7}{|c|}{\textbf{Experimental Architectures}} \\
    \hline
    A24 & Conv2DT* & 32 & 20 & 50K & 0.001 & None \\
    A25 & Conv2DT* & 32 & 11 & 50K & 0.001 & None \\
    \hline
    \end{tabular}
    
    \begin{flushleft}
    \footnotesize
    \textbf{Notes:}
    \begin{itemize}
        \item \textbf{Encoding Dim}: Smallest network dimension (bottleneck layer).
        \item \textbf{Samples}: Size of the training dataset.
        \item \textbf{LR}: Learning Rate.
        \item \textbf{ELR}: Exponential Learning Rate ($\gamma=0.95$).
        \item \textbf{RLROP}: Reduce Learning Rate on Plateau ($factor=0.8$).
        \item \textbf{Conv2DT*}: Uses transposed convolution in the encoder (atypical architecture).
        \item All models use Adam optimizer, MSE loss, a batch size of 64, ReLU activation for hidden layers, and Sigmoid for the output layer.
    \end{itemize}
    \end{flushleft}
    \end{table}
    
    \subsubsection{Evaluation Metrics}To quantitatively assess the performance of the despeckling filter, we used statistical metrics based on the multiplicative nature of speckle noise. Assuming the noisy image $z$ is the product of the clean image $x$ and the speckle noise $y$ ($z = x \cdot y$), the ratio image $I = z / \hat{x}$ (where $\hat{x}$ is the filtered image) should ideally contain only speckle noise with no remaining structural information.

Based on this principle, we used two metrics proposed by \cite{gomez2017unassisted}:
\begin{itemize}
    \item \textbf{First-order statistics ($r_{\textnormal{ENL},\mu}$):} This metric evaluates the preservation of the mean ($\mu_I$) and the Equivalent Number of Looks (ENL) in N homogeneous regions of the ratio image. It is defined as:
    \begin{equation}
        r_{\text{ENL},\mu} = \frac{1}{2N}\sum_{i=1}^{N} \left( \frac{\left|\text{ENL}_z(i) - \text{ENL}_I(i)\right|}{\text{ENL}_z(i)} + \left|1-\mu_I(i)\right| \right).
    \end{equation}
    A good filter should yield a ratio image with a mean close to 1 and preserve the ENL, resulting in a low $r_{\text{ENL},\mu}$.

    \item \textbf{Second-order statistics ($\delta_h$):} This metric measures residual geometric structure in the ratio image. It uses the homogeneity descriptor ($h$) from the Gray-Level Co-occurrence Matrix (GLCM). The metric $\delta_h$ calculates the percentage variation of the homogeneity of the original ratio image ($h_0$) compared to the average homogeneity of several randomly permuted versions of it ($\bar{h}_m$):
    \begin{equation}
        \delta_h = 100 \cdot \left| 1 - \frac{\bar{h}_m}{h_0} \right|.
    \end{equation}
    For a structureless ratio image, this value should be close to zero.
\end{itemize}

Finally, we combine these two metrics into a single figure of merit, $\mathcal{M} = r_{\textnormal{ENL},\mu} + \delta_h$, where a lower value indicates better despeckling performance.
